# Local development example environment variables
# Copy to .env.local (or use your preferred approach) and adjust values as needed.

# API gateway base URL (use Traefik HTTPS entrypoint by default for local dev)
REACT_APP_API_BASE_URL=https://localhost:8443
